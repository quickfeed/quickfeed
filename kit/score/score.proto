syntax = "proto3";
package score;
option go_package = "github.com/autograde/quickfeed/kit/score";

import "google/protobuf/timestamp.proto";
import "patch/go.proto";

// Score give the score for a single test named TestName.
message Score {
    uint64 ID = 1;
    uint64 SubmissionID = 2;
    string Secret = 3;       // the unique identifier for a scoring session
    string TestName = 4;     // name of the test
    int32 Score = 5;         // the score obtained
    int32 MaxScore = 6;      // max score possible to get on this specific test
    int32 Weight = 7;        // the weight of this test; used to compute final grade
    string TestDetails = 8;  // if populated, the frontend may display additional details (TODO(meling) adapt to output from go test -json)
}

// BuildInfo holds build data for an assignment's test execution.
message BuildInfo {
    uint64 ID = 1;
    uint64 SubmissionID = 2;
    google.protobuf.Timestamp BuildDate = 3 [(go.field) = {tags: 'gorm:"-"'}];
    string dbBuildDate = 4;  // the build date as string to be stored in the database
    string BuildLog = 5;
    int64 ExecTime = 6;
}

// Results holds a map from test names to score objects and build info.
// TODO(meling) to be removed
message Results {
    uint64 ID = 1;
    BuildInfo BuildInfo = 2 [(go.field) = {tags: 'gorm:"foreignKey:ID"'}];   // build info for tests
    repeated Score Scores = 3 [(go.field) = {tags: 'gorm:"foreignKey:ID"'}]; // list of scores for different tests
}
