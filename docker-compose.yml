services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - 443:443
    volumes:
      - vol:/app
volumes:
  vol:
    driver: local
    driver_opts:
      type: none
      device: .
      o: bind
